Get Elemento por ID.
---
parameters:
  - name: elemento
    in: elemento
    type: integer
    required: true

responses:
  200:
    description: Lista com o Elemento correspondente.
    schema:
      type: array
      items:
        $ref: '#/definitions/Elemento'
